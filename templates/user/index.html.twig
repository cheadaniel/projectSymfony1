{% extends 'base.html.twig' %}

{% block title %}User Dashboard!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<div>
			<h1>Pseudo :
				{{app.user.pseudo}}</h1>
			<h2>Email :
				{{app.user.email}}</h2>
			<h3>Amount :
				{{app.user.bank.amount}}</h3>
		</div>
		{{include('address/show.html.twig')}}
		{{include('annonce/show.html.twig')}}

	</div>
{% endblock %}
